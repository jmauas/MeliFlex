<div class="container">
    <div id="ventana">         
      <div class="container m-3">
        <div class="row align-items-center ">
          <div class="col col-2">
            <span>Tipo Envío</span>
          </div>
          <div class="col col-10">
            <select id="tipos" class="form-select form-select-lg m-3 btn-info" onchange="cambioTipo()" aria-label=".form-select-lg example">
              <% envios.forEach((envio) => {%>
                 <option value="<%= envio.IdP %>"><%= envio.Descrip %></option>    
              <% }) %>
            </select>
          </div>
        </div>
      </div>
      <%- include('../partials/camara') %>
    </div>
</div>